# simple-website-test — Specification v1

**Status:** Accepted (2026-09-04 — решения по §3 приняты Lily: тема/ФИО «ок», архитектура A локально-first; hosting: локальный запуск по умолчанию, деплой-готовность через Render/Procfile)
**Date:** 2026-09-04
**Author:** Lily (via Hermes)
**Repo:** H:\Work\simple-website-test (local Windows 10)

## 1. Overview & goal
Тестовое задание / демо: продающий лендинг из 2 экранов с формой заявки.
После отправки заявки: (1) письмо-уведомление уходит на superhumansmm@yandex.ru
по шаблону из ТЗ, (2) пользователь перенаправляется в Telegram-бота, который
благодарит за регистрацию. Всё должно запускаться локально одной командой и быть
готово к показу проверяющему.

## 2. Binding task contract
- **Owner/authority:** Lily. Реализация может делегироваться агенту по milestone'ам после Accepted.
- **Outcome:** работающий локальный стенд (лендинг + бот + письма), удовлетворяющий каждому AC из §10.
- **Scope:** лендинг 2 экрана, форма (имя + телефон), SMTP-уведомление, TG-бот с благодарностью, редирект на бота.
- **Non-goals (v1):** админка, CRM, БД заявок, хранение истории, мульти-лендинги, деплой на боевой домен (только если отдельно решит Lily).
- **Acceptance/stop condition:** все AC §10 зелёные + подтверждение Lily, что письмо реально дошло на указанный ящик.
- **Work packages:** §12 M0–M4, каждый с отдельным коммитом и exit-критериями.

## 3. Key decisions & alternatives considered
| Решение | Выбор | Почему / отклонённые варианты |
|---|---|---|
| Архитектура доставки | **A: Flask-мини-бэкенд** (локально-first, деплой-готовность: Procfile/env для Render). Выбрано Lily 2026-09-04 | A) Flask: лендинг + POST /api/lead + SMTP; бот — polling-скрипт. B) Статический HTML + Web3Forms — отклонён: access-key сервиса подтверждается письмом ВНУТРИ ящика получателя (superhumansmm@yandex.ru — чужой ящик), доставить туда не сможет. C) n8n — отклонён: 2 тривиальные автоматизации, нужен публичный webhook, лишний сервер; редирект всё равно клиентский. |
| Тема/наполнение лендинга | Нейтральный продающий лендинг услуг (тёмная тема) | Принято Lily («alright»). |
| Язык сайта | Русский | ТЗ и клиент русскоязычные. |
| Бот: способ получения сообщений | Long polling (getUpdates), stdlib urllib | Не нужен публичный URL/webhook — работает локально и за VPN; n8n/Telegram-webhook требуют публичного адреса. |
| Персонализация благодарности | t.me/<bot>?start=<транслит-имя> | Telegram допускает в start только [A-Za-z0-9_-] ≤64 — имя транслитерируется на клиенте; без payload бот благодарит обобщённо. |
| SMTP-отправитель | Ящик Lily + «пароль приложения» (2FA), smtp.yandex.ru:465 SSL | Web3Forms/Formspree неприменимы (см. выше). «Токен» из вопроса Lily = пароль приложения Yandex. |
| UI-стандарты | Тёмная тема, текст ≥ 12–14 px | Стоящие предпочтения Lily (8–10 px отклонено ранее). |
| Язык реализации | Python 3.11 (системный C:\Python311) + Flask; tzdata для zoneinfo на Windows | Уже стоит; venv --system-site-packages; без тяжёлых зависимостей. |

## 4. User scenarios
1. Пользователь открывает лендинг → экран 1 (оффер + CTA), скролл/кнопка → экран 2 с формой.
2. Вводит имя и телефон, жмёт «Отправить» → валидация проходит → показывается «Спасибо, заявка принята!» → через ~2 с редирект в Telegram-бота → жмёт Start → бот: «Спасибо, <имя>! Заявка принята, мы свяжемся с вами».
3. Владелец получает на superhumansmm@yandex.ru письмо: «Новая заявка с сайта <URL>», «Автор: <ФИО>», имя, телефон, время.
4. Пользователь вводит мусор/пустой телефон → inline-ошибка, письмо НЕ уходит, редиректа нет.
5. (Демо для проверяющего) Lily запускает `python app.py` + `python bot.py` → всё работает локально; письмо при тестировании уходит на её тестовый ящик (переключатель TEST_TO_EMAIL).

## 5. Functional requirements
- **FR-1 (лендинг):** 2 экрана в одном HTML (секции), тёмная тема, адаптивно (desktop + mobile ≥360 px), весь текст ≥ 12 px (заголовки крупнее). Экран 1: заголовок-оффер, подзаголовок, CTA-кнопка «Оставить заявку» → плавный скролл к экрану 2. Экран 2: форма и контакты.
- **FR-2 (форма):** поля «Имя» (обязательно, текст) и «Телефон» (обязательно, маска/валидация: 10–15 цифр, допускает +, -, пробелы, скобки). Кнопка «Отправить». Значения полей берутся как есть (безопасный экранинг при выводе).
- **FR-3 (отправка):** POST /api/lead {name, phone, comment?}. Сервер: валидирует повторно, формирует письмо по шаблону FR-5, отправляет SMTP. Успех → JSON {ok: true}; ошибка валидации → 400 {ok: false, error}.
- **FR-4 (редирект):** при ok:true клиент показывает экран «Спасибо» и через ~2 с делает location = https://t.me/<BOT_USERNAME>?start=<транслит имени>. Имя транслитерируется в [A-Za-z0-9_-] (≤64) — ограничение Telegram; без имени/бота параметр опускается, остаётся экран «Спасибо».
- **FR-5 (письмо, текст ровно по ТЗ):**
  ```
  Новая заявка с сайта <SITE_URL>

  Автор: <AUTHOR_FIO>

  Имя: <name>
  Телефон: <phone>
  Время: <DD.MM.YYYY HH:MM (Europe/Moscow)>
  ```
  Отправляется на TO_EMAIL (superhumansmm@yandex.ru), тема «Новая заявка с сайта <SITE_URL>». Тема письма и поля — конфиг-переменные, не хардкод.
- **FR-6 (бот):** long-polling скрипт. На /start: если есть payload — «Спасибо, <имя>! Ваша заявка принята, мы скоро свяжемся с вами.»; без payload — общее «Спасибо за регистрацию! ...». На любое другое сообщение — вежливый ответ-заглушка. Одна благодарность за сессию не требуется повторно.
- **FR-7 (конфиг):** config.json (или env-переменные — приоритетнее) рядом с кодом: SITE_URL, AUTHOR_FIO, SMTP_HOST/PORT/USER/APP_PASSWORD, TO_EMAIL, TEST_TO_EMAIL (если задан — письма уходят туда, а не клиенту), BOT_TOKEN, BOT_USERNAME. Толерантное чтение: неизвестные ключи игнорируются, отсутствующие → default или явная ошибка в момент использования функции (dev/тесты работают без секретов). config.json в .gitignore; в репо — config.example.json.

## 6. Non-functional requirements
- Локальный запуск на Windows 10: `pip install -r requirements.txt` (flask, pytest, gunicorn, tzdata), `python app.py` (лендинг :5000), `python bot.py` (stdlib urllib — дополнительных зависимостей не тянет).
- Письмо уходит за ≤ 5 с после POST при живом SMTP; при недоступности SMTP — 502 {ok:false}, клиент видит «не получилось, попробуйте позже», редиректа нет (никаких потерянных заявок молча).
- Зависимостей минимум; работает через VPN/толстый линк Lily.
- Код без секретов в git: config.example.json в репо, реальный config.json в .gitignore.

## 7. Architecture
```
[Браузер] --GET /--> Flask app.py --serves--> index.html (2 экрана, JS)
[Браузер] --POST /api/lead--> app.py --SMTP--> superhumansmm@yandex.ru
[Браузер] --redirect t.me/bot?start=имя--> Telegram --> bot.py (polling) --> "Спасибо, имя!"
```
- app.py НЕ хранит заявки (non-goal), только валидирует и шлёт письмо.
- bot.py самостоятелен, общается только с Telegram API; знать о сайте не обязан (имя приходит в payload /start).

## 8. Contracts
- POST /api/lead: body JSON {name: str 1..100, phone: str 10..15 digits после очистки}. Ответы: 200 {ok:true}; 400 {ok:false, error: "invalid_name"|"invalid_phone"}; 502 {ok:false, error:"smtp"}.
- config.json (пример в репо как config.example.json):
  ```json
  {
    "SITE_URL": "http://127.0.0.1:5000",
    "AUTHOR_FIO": "Гринина Лилия",
    "TO_EMAIL": "superhumansmm@yandex.ru",
    "TEST_TO_EMAIL": "",
    "SMTP_HOST": "smtp.yandex.ru",
    "SMTP_PORT": 465,
    "SMTP_USER": "your_login@yandex.ru",
    "SMTP_APP_PASSWORD": "",
    "BOT_TOKEN": "",
    "BOT_USERNAME": ""
  }
  ```
- Тема письма: `Новая заявка с сайта <SITE_URL>`.

## 9. Environment & setup
- Python 3.11 (C:\Python311, системный), venv с --system-site-packages; зависимости: flask, pytest, gunicorn (для Render), tzdata (zoneinfo на Windows) — малый набор.
- Пакет: `tree`:
  ```
  app.py            # Flask: лендинг (GET /) + /api/lead + SMTP
  bot.py            # polling-бот (stdlib)
  config.py         # толерантный читатель config.json + env
  templates/index.html  # лендинг: 2 экрана, форма, JS
  tests/            # pytest: письмо через фейковый SMTP, валидация, bot-handler
  config.example.json
  requirements.txt
  Procfile
  README.md
  SPEC.md
  ```

## 10. Acceptance criteria
Автоматизированные (pytest, офлайн, без реальной отправки):
- AC-1: POST /api/lead с валидными данными и подменённым SMTP-отправителем (mock) → письмо содержит строки «Новая заявка с сайта …», «Автор: …», «Имя: …», «Телефон: …» и тему с URL; TO_EMAIL = получатель.
- AC-2: POST с пустым именем / телефоном «abc» → 400, письмо не отправлено.
- AC-3: bot-handler на /start с payload «Иван» → текст ответа содержит «Иван»; без payload → содержит «Спасибо за регистрацию».
- AC-4: GET / возвращает HTML с двумя секциями-экранами и формой (name/phone присутствуют).
Ручные:
- AC-5: в браузере: экран 1 → CTA → экран 2 → форма → отправка → «Спасибо» → редирект в бота (если BOT_USERNAME задан) → Start → персональная благодарность.
- AC-6: письмо реально доходит на ящик (при тестах — на TEST_TO_EMAIL) с текстом по FR-5.
- AC-7: невалидный телефон показывает ошибку рядом с полем, письма нет.

## 11. Risks & open questions
- **Риск: письма в спаме Yandex.** Митигация: тест на TEST_TO_EMAIL сначала; если спамится — тема/домен/отправитель, потом решаем (альтернатива Web3Forms).
- **Риск: throttled линк Lily** — малые зависимости, pip --timeout 120 --retries 10.
- Решено по ходу: архитектура A (§3), тема «нейтральные услуги», ФИО «Гринина Лилия», /start-персонализация — реализована (транслит payload).
- **OPEN (единственный): hosting для демонстрации** — по умолчанию локальный запуск (Lily: «нужно как-то демонстрировать/хостить»); код деплой-готов (Procfile, env-конфиг) для Render/др. Решение за Lily при сдаче проекта.

## 12. Work packages / milestones
- **M0 — scaffold:** SPEC.md принят, config.py + config.example.json + Flask-каркас, GET / отдаёт index.html-заглушку с 2 секциями. *Exit:* pytest AC-4 зелёный, `python app.py` отвечает 200.
- **M1 — почта:** /api/lead + валидация + SMTP-отправка по шаблону. *Exit:* AC-1, AC-2 зелёные (mock SMTP); ручная проверка письма на TEST_TO_EMAIL.
- **M2 — бот:** bot.py long-polling, /start c payload и без. *Exit:* AC-3 зелёный; живой прогон с реальным токеном (даст Lily).
- **M3 — UI:** финальный дизайн 2 экранов (тёмная тема, ≥12 px), JS-валидация, экран «Спасибо», редирект. *Exit:* AC-5, AC-7 вручную; скриншоты 2 экранов.
- **M4 — приёмка:** AC-6 реальным письмом, полный прогон сценариев §4, README с инструкцией запуска. *Exit:* все AC зелёные, sign-off Lily.

## 13. Definition of done for the spec itself
- [x] Lily выбрала решения §3 (тема «ок», ФИО «ок», архитектура A; hosting — OPEN до сдачи, default локально).
- [x] Lily ревьюила §5/§10 по ходу рефайна; правки после живых прогонов внесены (транслит start-payload, tzdata, mock-SMTP-тесты).
- [x] SPEC помечен **Accepted**; реализация M0–M3 закоммичена, M4 (живое письмо/бот) — после кредов Lily.
